{
  "name": "uxele-core",
  "version": "1.1.0",
  "description": "Core types, interfaces of uxele",
  "main": "build/index.js",
  "scripts": {
    "test": "karma start",
    "test:human": "INTERACTIVE=true karma start",
    "dev": "tsc -w",
    "build": "rm -Rf ./build && tsc && cpx ./src/**/*.js ./build",
    "dep:local": "echo 'no dep'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^2.9.2",
    "cpx": "^1.5.0"
  },
  "dependencies": {
    
  }
}
